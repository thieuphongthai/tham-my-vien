const express = require('express');
const router = express.Router();
const OperationRoomController = require('../app/controllers/Operation_roomController');

// router.get('/', OperationRoomController.getOperationDashboard);

module.exports = router;